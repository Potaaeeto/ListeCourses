<style>
    .btn{
        color: #147cfc;
        margin-left: 10px;
    }
    .btn:hover{
        color: #0454b4;
        text-decoration: underline;
    }
</style>

<form method="post" action="{{ path('app_liste_delete', {'id': liste.id}) }}"
      onsubmit="return confirm('Etes vous sûre de vouloir supprimer cette liste de course?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ liste.id) }}">
    <button class="btn">Supprimer</button>
</form>

